# 高德地图伴侣 - 构建说明

## 快速开始

### 方式一：使用一键构建脚本（推荐）

1. **双击运行** `build_with_config.bat` 即可开始构建！

### 方式二：配置后构建

1. 复制 `build_config.bat.example` 为 `build_config.bat`
2. 编辑 `build_config.bat`，根据你的环境修改路径
3. 双击运行 `build_with_config.bat`

## 环境要求

- Windows 系统
- Android SDK（已在 `D:\Android\SDK`）
- JDK 17（已在 `D:\Android\Java\jdk-17`）

## 文件说明

| 文件名 | 说明 |
|--------|------|
| `build_with_config.bat` | 推荐使用的一键构建脚本 |
| `build_config.bat.example` | 配置文件模板 |
| `build.bat` | 基础构建脚本 |
| `build.ps1` | PowerShell 构建脚本 |
| `debug.keystore` | 调试签名密钥 |

## 构建输出

构建成功后会在当前目录生成：
- `amap_companion_signed.apk` - 已签名的 APK 文件

## 功能特性

✓ 车载桌面启动器
✓ 画中画模式
✓ 分屏功能
✓ 高德地图悬浮窗
✓ 开机自动启动

## 常见问题

**Q: 找不到 Android SDK？**
A: 复制 `build_config.bat.example` 为 `build_config.bat` 并修改 `ANDROID_HOME` 变量

**Q: 构建失败？**
A: 检查环境是否安装了 JDK 17 或更高版本

## 开发者配置示例

build_config.bat 示例：
```batch
set ANDROID_HOME=D:\Android\SDK
set JAVA_HOME=D:\Android\Java\jdk-17
```
